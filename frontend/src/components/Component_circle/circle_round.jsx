import React from "react";
import styles from './circle_round.module.css';

function Circle() {
    return (
        <div className={styles.spinner}>
            <div className={styles.spinner1} />
        </div>
    );
}

export default Circle;
